import SwiftUI

struct RepositoriesView: View {
    var body: some View {
        NavigationStack {
            Text("Repositories")
                .navigationTitle("Repositories")
        }
    }
}
